Temporarily remove placeholder testimonials, add CLAUDE.md to remember

Maya wants to share the site with friends but the testimonials section
still has [bracketed] placeholder quotes -- if anyone re-shares the site
publicly those would read as the real thing. Removed for now.

What's gone:
- The .testimonials-section CSS block
- The corresponding mobile media-query rules
- The <section class="testimonials-section"> HTML block
  (was between the How-it-works section and the order form)

How to restore:
- The design is preserved at git tag `testimonials-design-saved`
  (commit 0fd36fa, pushed to origin). `git show testimonials-design-saved`
  to see the exact code.
- A placeholder HTML comment now marks the slot in index.html.

Added CLAUDE.md so future Claude Code sessions automatically pick up
the context: any site review or improvement audit will flag the
deliberately-missing testimonials section as something to revisit.
